import { ExchangeRateResponse } from './exchange-rate-response';

describe('ExchangeRateResponse', () => {
  it('should create an instance', () => {
    expect(new ExchangeRateResponse()).toBeTruthy();
  });
});
